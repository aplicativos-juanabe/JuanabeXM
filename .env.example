# Servidor
PORT=3001
NODE_ENV=development

# CORS y seguridad
FRONTEND_URL=http://localhost:3000
ADMIN_API_KEY=changeme_replace_it_in_production

# API (para cliente)
VITE_API_URL=http://localhost:3001

# Base de datos (si migras a Postgres en Railway)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# Para SQLite local (default)
DATABASE_PATH=./server/exam.db